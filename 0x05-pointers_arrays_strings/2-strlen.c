#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @str: the string thats length is needed
 * Return: The length of @str
 */
int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
	length++;
	return (length);
}

